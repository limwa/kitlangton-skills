{
  "name": "kit-skills",
  "private": true,
  "scripts": {
    "check": "bun run validate && bun run typecheck",
    "typecheck": "tsc --noEmit",
    "validate": "bun scripts/validate.ts"
  },
  "devDependencies": {
    "effect": "4.0.0-beta.97",
    "typescript": "^5.9.2"
  }
}
